{
  "name": "js-json-go",
  "version": "0.1.2",
  "description": "Js-JSON-Go is a Javascript library intending to make it easier to handle (and query) JSON messages, especially to grap (get) and update (set) parts of a larger JSON message, and translate (map) these to another path.",
  "main": "index.js",
  "scripts": {
    "test": "jest --collect-coverage"
  },
  "keywords": [
    "json",
    "get",
    "set",
    "map",
    "event-based",
    "internet-of-things"
  ],
  "author": "JG-1202",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "jest": "^26.6.3"
  }
}
