export = getPlaceholders;
/**
 * Get all placeholders that are not (part of) a string
 * @param {string} string
 * @returns {string[]} all =(X) that are not strings
 */
declare function getPlaceholders(string: string): string[];
