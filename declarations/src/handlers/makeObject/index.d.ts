export = makeObject;
/**
 * Checks whether value is an Object, if so return value, otherwise return empty object
 * @param {any} value - any value
 * @returns {Object}
 */
declare function makeObject(value: any): any;
