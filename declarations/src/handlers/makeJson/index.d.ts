export = makeJson;
/**
 * Checks whether value is an Object or an Array, if so return value, otherwise return empty object
 * @param {any} value - any value
 * @returns {(Object|Array)}
 */
declare function makeJson(value: any): (any | any[]);
